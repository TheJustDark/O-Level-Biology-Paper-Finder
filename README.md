Donwload the Scrapper.py and run it, specify the folder on which the files are going to be download to.(Manually help it go pass the security check if it shows up)
Download the inventory.json file and put it on the same directory as the papers
Download the latest release and put it on the same directory as the ones before and run it
